---
id: k8s
title: Kubernetes
sidebar_label: Kubernetes
---

Random K8s notes

```
kubectl explain node.spec // show all the spec of the running k8s version for Node
kubectl explain node --recursive // show all the spec of the running k8s version for Node, helpful when you can't lookup the online doc.
kubectl get pods --all-namespaces
kubectl get pods --namespace=<name>
```
